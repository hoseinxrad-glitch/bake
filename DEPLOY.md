# راهنمای انتشار (Deploy) روی گیت‌هاب پیجز + رندر

این راهنما توضیح می‌دهد چطور بک‌اند را روی **Render** (رایگان) و فرانت‌اند را روی **GitHub Pages** (رایگان) منتشر کنی.

## گام ۱ — آپلود پروژه روی گیت‌هاب

اگر هنوز پروژه را روی گیت‌هاب نگذاشته‌ای:

```bash
cd messaging-app
git init
git add .
git commit -m "اولین نسخه"
```

بعد یک ریپازیتوری خالی روی [github.com/new](https://github.com/new) بساز و طبق راهنمای خودِ گیت‌هاب پوش کن (`git remote add origin ...` و `git push`).

## گام ۲ — دیپلوی بک‌اند روی Render

1. وارد [render.com](https://render.com) شو و با گیت‌هاب لاگین کن.
2. **New +** → **Web Service** را بزن و ریپازیتوری خودت را انتخاب کن.
3. تنظیمات زیر را بگذار:
   - **Root Directory:** `backend`
   - **Runtime:** Python 3
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `uvicorn main:app --host 0.0.0.0 --port $PORT`
4. در بخش **Environment Variables**، متغیرهای لازم را اضافه کن (همان‌هایی که قبلاً با `set` روی سیستم خودت تنظیم می‌کردی)، مثلاً:
   - `GAPGPT_API_KEYS` = کلیدهای خودت با کاما جدا شده
   - `GAPGPT_MODEL` = اسم مدلی که پیدا کردی
   - `ADMIN_PANEL_PASSWORD` = یک رمز جدید و امن (پیشنهاد می‌شود پیش‌فرض `132465798` را عوض کنی)
5. **Create Web Service** را بزن. بعد از چند دقیقه، یک آدرس شبیه این می‌گیری:
   ```
   https://chatyar-backend.onrender.com
   ```

> **نکته‌ی مهم درباره‌ی دیتابیس:** پلن رایگان Render دیسک را بین دیپلوی‌ها (و گاهی بعد از مدتی بی‌کاری) پاک می‌کند؛ یعنی فایل `chat.db` و فایل‌های آپلودشده ممکن است از بین بروند. برای یک پروژه‌ی آزمایشی/شخصی مشکلی نیست؛ برای استفاده‌ی جدی، دیتابیس را به Postgres (مثلاً از طریق خود Render یا Supabase/Neon) مهاجرت بده.

> **نکته‌ی دیگر:** پلن رایگان Render بعد از چند دقیقه بی‌کاری خواب می‌رود (cold start) و اولین درخواست بعد از خواب ممکن است ۳۰-۵۰ ثانیه طول بکشد — طبیعی است.

## گام ۳ — تنظیم آدرس بک‌اند در فرانت‌اند

فایل `frontend/config.js` را باز کن و آدرس Render خودت را بگذار:

```js
const API_BASE = "https://chatyar-backend.onrender.com";
const WS_BASE = "wss://chatyar-backend.onrender.com";
```

(دقت کن: `WS_BASE` باید با `wss://` شروع شود، نه `ws://`، چون Render همه‌چیز را روی HTTPS سرو می‌کند.)

تغییر را کامیت و پوش کن:
```bash
git add frontend/config.js
git commit -m "تنظیم آدرس بک‌اند برای دیپلوی"
git push
```

## گام ۴ — دیپلوی فرانت‌اند روی GitHub Pages

1. وارد تنظیمات ریپازیتوری روی گیت‌هاب شو: **Settings → Pages**
2. زیر «Build and deployment»، منبع (Source) را روی **Deploy from a branch** بگذار.
3. شاخه (Branch) را `main` و پوشه را `/frontend` انتخاب کن (اگر گزینه‌ی انتخاب پوشه‌ی دلخواه نبود، محتوای پوشه‌ی `frontend` را به‌جای کل ریپو در یک ریپازیتوری جدا، یا در پوشه‌ی `/docs` قرار بده و همان را انتخاب کن).
4. **Save** بزن. بعد از یکی-دو دقیقه، آدرس سایتت چیزی شبیه این می‌شود:
   ```
   https://username.github.io/messaging-app/
   ```

## گام ۵ — تست نهایی

آدرس گیت‌هاب پیجز را باز کن، ثبت‌نام کن، و مطمئن شو پیام‌ها، دستیار هوشمند، و پنل ادمین کار می‌کنند. اگر مشکلی بود، ابتدا کنسول مرورگر (F12) را چک کن — بیشتر مشکلات ناشی از اشتباه بودن آدرس در `config.js` یا فراموش کردن `wss://` به‌جای `ws://` است.

## نکات امنیتی قبل از انتشار عمومی واقعی

- `ADMIN_PANEL_PASSWORD` را حتماً از مقدار پیش‌فرض عوض کن.
- `SECRET_KEY` در `backend/auth.py` را با یک مقدار تصادفی و طولانی جایگزین کن (بهتر است آن را هم به‌عنوان Environment Variable در Render بگذاری، نه داخل کد).
- `allow_origins=["*"]` در CORS را می‌توانی بعداً به آدرس دقیق گیت‌هاب پیجز خودت محدود کنی برای امنیت بیشتر.
